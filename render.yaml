services:
  - type: web
    name: omniroute
    runtime: image
    image:
      url: ghcr.io/alwaysitspeace/omniroute-render:latest
    plan: free
    region: virginia
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOSTNAME
        value: 0.0.0.0
